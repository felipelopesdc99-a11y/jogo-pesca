# Base URL of the Fishing Idle backend. The console reads project status from it.
FISHING_IDLE_API_BASE_URL=http://localhost:5080

# Absolute path to the repository root, used as the fallback source for docs/roadmap.json
# when the backend is unreachable. Leave empty to let the console discover it by walking up.
FISHING_IDLE_REPO_ROOT=
